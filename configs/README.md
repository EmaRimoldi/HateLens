# Configuration files

YAML files in `models/` are passed to `hatelens train <path> --dataset <dynahate|hatecheck>`.

Training artifacts are written under `outputs/runs/<model_stem>/<dataset>/` (see `hatelens.train_pipeline`).
